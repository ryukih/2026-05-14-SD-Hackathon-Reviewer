# 評価検証レポート: reference

**検証日**: 2026-05-11
**対象**: `reference_eval.md`
**元評価合計**: 30.5/40

---

## 検証サマリー

| カテゴリ | 元スコア | 検証結論 | 推奨スコア帯 |
|---|---|---|---|
| A. 創発設計 | 8.5 | 妥当 | 8-9 |
| B. 世界設定 | 7.0 | 妥当 | 6-7 |
| C. 発展性 | 7.0 | 妥当 | 6-7 |
| D. 技術実装 | 8.0 | 妥当 | 7-8 |

**総合判定**: 妥当

---

## カテゴリ別検証

### A. 創発設計（元: 8.5/10）

**根拠の検証**:
- `agent.py:254-281` — ✓実在・記述と一致。L254 で `You are Agent {self.id} ({self.gender})` が始まり、L272 で "observations, experiences, or thoughts" の文言、L274 以降で JSON 出力スキーマ。占有率/数値のみ提示で定性表現なし。
- `agent.py:347-382` — ✓実在・記述と一致。L347 が行動プロンプトの先頭で、L367 に `=== AVAILABLE ACTIONS ===`、L368-369 に "stay"/"move" の機械的列挙のみで戦略誘導なし。
- `agent.py:174-193` — ✓実在・記述と一致。`_build_fire_section` メソッド本体で、コメント (L177-178) に "No qualitative descriptions (e.g. 'dangerous', 'evacuate') are included" と明示されている。出力文字列は Position/Intensity/Radius/Your distance のみ。
- `simulation.py:299-322` — ✓実在・記述と一致。`get_fire_info_for_agent` は L299 から始まり、半径外なら `perceived` に追加されず None を返す Model B 実装。
- README "定量情報のみ: ..." — ✓README L19 に完全一致で存在。
- README "同一領域条件: 場所内のエージェント ↔ 場所外のエージェント: 通信不可" — ⚠軽微な不整合。実際の README L420 は "**同一領域条件**:" で、L425 に "❌ 場所内のエージェント ↔ 場所外のエージェント: **通信不可**" として、評価が結合した形で記述。意味は完全に一致するため引用としては妥当だが、原文の正確なフレーズではなく要約結合。

**スコア妥当性**:
ルーブリック上位帯（9-10: 精密ルール + 生データのみ）に近い実装。火災知覚モデル B、通信制限、占有率の非対称配布が体系的で、プロンプトも数値のみ。一方、メッセージ用プロンプト L272 の "observations, experiences, or thoughts" や、`memory` フィールドの "your thoughts, observations, intentions"（L377）は微弱なメタ的ヒントとして残存。8.5 は妥当だが 9.0 寄りでも擁護可能。

**見落とし・誇張**:
- 見落とし: `agent.py:95-118` の `get_nearby_agents` における「同一領域条件」のコード実装（L112-115 の `same_area` 判定）が、世界ルールの精度を裏付ける重要証拠であるが言及されていない。
- 誇張: 特になし。

**検証結論**: 妥当

---

### B. 世界設定（元: 7.0/10）

**根拠の検証**:
- README "エージェント自身が状況を解釈し..." — ✓README L5 に存在し記述と一致。
- README "デモ動画：火災近傍では..." — ✓README L12 に完全一致で存在。
- `config.yaml:21-33` — ✓実在・記述と一致。L21-27 に left_bar (capacity=12)、L28-33 に right_bar (capacity=10) で非対称容量が確認できる。
- `agent.py:254` — ✓実在・記述と一致。`You are Agent {self.id} ({self.gender})` のテンプレート。
- `simulation.py:299-322` — ✓上述の通り実在。

**スコア妥当性**:
題材は「バー × 火災」で、社会科学（群集動学、情報拡散）への対応は明瞭だがありふれた領域。性別ペルソナ・容量非対称・通信ゾーン制限は工夫のある設計だが、ルーブリック 9-10 の「highly original」には至らない。7.0 は妥当。6 寄りでも擁護可能。

**見落とし・誇張**:
- 見落とし: 性別ペルソナを社会科学的設定の文脈で言及している一方、実装上は単なる属性表示（L143-152）であり、行動分化のメカニズムを伴わない点には触れていない。
- 誇張: 「社会科学的に意義ある問いを内包」は控えめな表現で妥当範囲内。

**検証結論**: 妥当

---

### C. 発展性（元: 7.0/10）

**根拠の検証**:
- `utils.py:7-24` — ✓実在・記述と一致。`FireConfig` (L7-14) と `PlaceConfig` (L17-24) の TypedDict 定義あり。
- `simulation.py:48-73` — ✓実在・記述と一致。`places` 設定のバリデーション（必須フィールドチェック含む）。
- `simulation.py:75-94` — ✓実在・記述と一致。`fires_config` リスト処理と複数火災対応。
- `config.yaml:18-33` — ✓実在・記述と一致。L18-20 にコメント、L21-33 に places 設定。
- README "将来の拡張例（カフェや図書館など）" — ✓README L234 に "将来の拡張例（カフェや図書館など）:" と存在。直後 L236-249 が YAML 例のみで研究的方向性の記述はない、という評価も実態に一致。
- `visualization.py:122-131` — ⚠軽微な不整合。実際の `place_type_colors` 辞書は L122-128 に定義（5種類: bar/cafe/library/restaurant/park）。L129-130 は `default_colors`（フォールバック）であり、評価が指定する L122-131 は前後を多めにとった範囲。内容の主張（5種を網羅）は正しい。

**スコア妥当性**:
コード拡張性は確かに高く、YAML 駆動・モジュール分離・TypedDict 化が揃っている。一方、READMEの将来展望は YAML 拡張サンプルのみで、研究仮説や評価指標の記述がない（実態確認済）。半々ルールの片方（コード）が強く、片方（ビジョン）が弱いという評価は正確で 7.0 は妥当。

**見落とし・誇張**:
- 見落とし: ランダムシード未設定のため設定再現性に課題があり、これは拡張性（実験再現比較）にも関わる。総評では言及されているが C カテゴリ本文では未言及。
- 誇張: 「新エージェント行動タイプの追加は `Agent` クラスのメソッド追加のみで対応可能」は、`DIRECTION_MAP`（agent.py:19-24）や `parse_action_response`（L467-498）への変更も必要なため、やや楽観的だが致命的ではない。

**検証結論**: 妥当

---

### D. 技術実装（元: 8.0/10）

**根拠の検証**:
- `agent.py:385-420` — ✓実在・記述と一致。`_extract_json_from_text` の括弧深度追跡実装。L393 で `depth = 0`、L413-418 で深度管理して対応する `}` を返す。
- `agent.py:500-547` — ✓実在・記述と一致。`decide_message` (L500-516) と `decide_action` (L518-547) の両方に try/except + fallback。
- `agent.py:530-542` — ✓実在・記述と一致。L536 に `memory_entry = f"Step {step}: {memory_content}"` が完全一致で存在。L540-542 で memory_limit による FIFO 管理。
- `simulation.py:156-176` — ✓実在・記述と一致。`_log_memory_reasoning_batch` メソッド本体。
- `simulation.py:324-418` — ✓実在・記述と一致。`step_simulation` は L324 開始で、コメントに Phase 1-4 が明示され（L361, L373, L391, L415）、L418 が Phase 4 の最終行近傍。
- `ollama_client.py:68-80` — ✓実在・記述と一致。L73-79 の `options` 辞書に `repeat_penalty`/`repeat_last_n`/`min_p` が含まれる。
- `main.py:63-79` — ✓実在・記述と一致。`determine_visualization_settings` 関数定義 (L63-79)。

**スコア妥当性**:
- LLM 利用（3点満点）: フェーズ分離プロンプト、JSON 抽出、try/except + fallback、反復抑制パラメータ調整、すべて確認できる。約 2.5/3。
- メモリ設計（3点満点）: LLM 自己生成メモリ、`memory_limit`/`memory_size` 分離、タイムスタンプ付与、すべて実装済。約 2.5/3。
- コード品質（2点満点）: 定数抽出、TypedDict、ヘルパー分離、バッチI/O が確認できる。約 1.8/2。
- シミュレーション正確性（2点満点）: 4フェーズ同期実行は明確（L361-415）、メッセージが移動前位置に基づく点も正しい。一方、ランダムシード未設定で再現性に難。約 1.2/2。

合計約 8.0/10 で妥当。元評価の指摘（シード未設定）は適切。

**見落とし・誇張**:
- 見落とし: 同期実行の良さは指摘されているが、`get_nearby_agents` で `agent.id` 比較を線形走査している点（agent.py:95-118）は規模拡大時の課題。技術的には軽微なので減点理由ではない。
- 見落とし: `ollama_client.py` の `requests.exceptions.RequestException` 専用 except 分岐（L92-94）も評価できる。
- 誇張: 「全LLM呼び出しに付いている」のフォールバック動作は実際に L510-516 / L528-547 に存在し正確。問題なし。

**検証結論**: 妥当

---

## 総合所見

- 元評価の品質: 全体として高品質。引用された 18 件の `filename:line_range` および README 引用のうち、ほぼすべてが実在・内容一致で、軽微な不整合（行範囲の前後拡張、引用語句の要約結合）は 2 件のみ。記述の精度は高く、過大評価・過小評価のいずれも見られない。優れている点・改善点のバランスも良好。
- 主要な指摘点:
  1. README からの「同一領域条件」引用は原文を要約結合した形になっており、厳密な逐語引用ではない（意味は正確）。
  2. `visualization.py:122-131` の行範囲は厳密には L122-128 が `place_type_colors`（L129-130 は default_colors）。主張は妥当。
  3. A カテゴリで `get_nearby_agents` の通信ルール実装（agent.py:95-118）への言及があれば、創発設計の根拠が一層強固になった。
- 推奨アクション: スコア・記述ともそのまま採用可能。引用フォーマットを厳密化する場合は上記 2 点を微修正するとさらに精度が上がる。
